---
const { imageSrc, altText, title, content } = Astro.props;
---

<div class=" bg-zinc-100 rounded-lg shadow-md transform hover:scale-105 transition-transform duration-300">
  <img src={imageSrc} alt={altText} class="mb-4 rounded-t-lg object-cover h-48 w-full">
  <h3 class="text-2xl font-bold mb-2 text-gray-800 p-2">{title}</h3>
  <p class="text-gray-600 p-2 mb-3">{content}</p>
</div>
